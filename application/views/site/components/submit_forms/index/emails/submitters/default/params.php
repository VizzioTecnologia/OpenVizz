<?php
	/*
	foreach ( $params[ 'params_spec' ][ 'ud_ds_emails_for_receivers' ] as $key => $element ) {
		
		if ( $element[ 'name' ] == 'articles_per_category_order_by' ){
			
			$spec_options = array();
			
			if ( isset( $params[ 'params_spec' ][ 'ud_ds_emails_for_receivers' ][ $key ][ 'options' ] ) )
				$spec_options = $params[ 'params_spec' ][ 'ud_ds_emails_for_receivers' ][ $key ][ 'options' ];
			
			$params[ 'params_spec' ][ 'ud_ds_emails_for_receivers' ][ $key ][ 'options' ] = is_array( $spec_options ) ? $spec_options + $order_by_options : $order_by_options;
			
			break;
			
		}
		
	}
	*/
?>